# Let's double check if we can write a clean JS module extension
# We will define a python script to test generating the zones.js content with ORDINANCES

print("Generating zones.js extensions test")
